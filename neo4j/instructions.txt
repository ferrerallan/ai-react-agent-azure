1) create vm
2) ssh -i neo4j-pc_key.pem azureuser@20.9.140.20
3) chmod 400 neo4j-pc_key.pem
4) ssh -i neo4j-pc_key.pem azureuser@20.x.x.x

inner VM:
1) wget -O - https://debian.neo4j.com/neotechnology.gpg.key | sudo apt-key add -
echo 'deb https://debian.neo4j.com stable 4.4' | sudo tee /etc/apt/sources.list.d/neo4j.list
sudo apt update

2) sudo apt install neo4j -y

3) sudo nano /etc/neo4j/neo4j.conf
Localize e descomente (ou adicione) a linha:
dbms.default_listen_address=0.0.0.0

4) sudo neo4j-admin set-initial-password neo123456


